# TPM clones tmux plugins into the repo when ~/.config/tmux is a symlink
tmux/.config/tmux/plugins/

# fish writes its universal variables here through the ~/.config/fish symlink
fish/.config/fish/fish_variables

# per-machine overrides (none currently; kept for apps.local if added later)
*.local
